public class ConsoleDriver {
    public static void main(String[] args) {
        // bulk of logic is elsewhere
        // this should only have IO logic
        // meaning talk to user, get user input, show changes to user

        // eg
        // create on object type Game, Adventure...
        // Game g = new Game();
        // maybe while(!g.hasWon()) {}
        // ask user for input
        // call g.displayStatus() --- or displayBoard()

        // can add GUI with minimal if any changes to game logic
    }
}
